# @app/domain

Shared **TypeScript** types, placeholder enums, and branded string ids for workspace packages.

Any `@app/*` package may depend on `@app/domain`. Changes are picked up by workspace symlinks; run `yarn typecheck` at the root or in dependents after breaking edits.
