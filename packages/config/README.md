# @app/config

Shared **ESLint** flat configs, **Prettier** preset, and **TypeScript** JSON extends (`tsconfig/base`, `react-native`, `node`). Consumed via `extends` / `require('@app/config/...')` in other workspaces—not imported as runtime JS.
